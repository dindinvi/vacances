
{% block body -%}
    <h1>MotsCle creation</h1>

    <form action="{{ path('motscle_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('motscle') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
