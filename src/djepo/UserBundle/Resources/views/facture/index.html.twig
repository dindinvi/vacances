{% extends '::base.html.twig' %}

{% block body -%}
    <h1>facture list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Dateemission</th>
                <th>Typeprestation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('facture_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titre }}</td>
                <td>{% if entity.dateEmission %}{{ entity.dateEmission|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.typePrestation }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('facture_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('facture_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('facture_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
