{% extends '::base.html.twig' %}

{% block body -%}
    <h1>etatDesLieux list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datevisite</th>
                <th>Avislocataire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('no_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateVisite %}{{ entity.dateVisite|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.avisLocataire }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('no_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('no_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('no_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
